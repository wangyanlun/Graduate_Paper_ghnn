# Graduate_Paper_ghnn
It's my graduate paper by follow the A generalized framework of structure-preserving neural networks for Hamiltonian systems

## Data

Large dataset files (e.g., `.h5`) are not included.

To reproduce results:
- Place your data in a local `Data/` directory, or
- Use the provided `generate_data.py` script in the 00_DATA_GENERATE.
